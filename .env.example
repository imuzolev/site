# Copy to .env.local to enable real video playback.
# Leave unset (or false) to render the animated gradient fallbacks with zero
# network requests — useful until you've added footage to public/videos.
NEXT_PUBLIC_ENABLE_VIDEO=false
